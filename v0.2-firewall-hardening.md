# v0.2 – Firewall Hardening

## Summary
The firewall has been enabled to reduce attack surface while maintaining required services.

## Changes from v0.1
- UFW enabled
- Default deny policy applied
- Explicit allows for SSH and HTTP

## Firewall Rules
- SSH (22/tcp): allowed
- HTTP (80/tcp): allowed
- All other inbound traffic: denied

## Verification
- SSH access confirmed after firewall enable
- Apache reachable via LAN IP
